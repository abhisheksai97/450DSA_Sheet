// postorder traversal.. (L-R-N)..
 void postorder(node* root){
     // base case..
     if(root==NULL){
         return;
     }
     postorder(root->left);
     postorder(root->right);
     cout<<root->data<<" "; 
 }
